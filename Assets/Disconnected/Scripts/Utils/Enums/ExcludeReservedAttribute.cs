using UnityEngine;

/// <summary>
/// Attribute to mark enum fields that should exclude reserved values
/// No parameters needed - automatically uses singleton!
/// </summary>
public class ExcludeReservedAttribute : PropertyAttribute
{
    // No parameters needed anymore!
}